---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: networkinterfaceattachments.juneau.loutres.me
spec:
  group: juneau.loutres.me
  names:
    kind: NetworkInterfaceAttachment
    listKind: NetworkInterfaceAttachmentList
    plural: networkinterfaceattachments
    shortNames:
    - niattach
    - nia
    singular: networkinterfaceattachment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.networkInterfaceRef
      name: Interface
      type: string
    - jsonPath: .spec.podRef.name
      name: Pod
      type: string
    - jsonPath: .spec.nodeName
      name: Node
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkInterfaceAttachment is the Schema for pod-scoped interface
          bindings.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              NetworkInterfaceAttachmentSpec binds one persistent NetworkInterface to a
              concrete Pod interface on a node. Attachments are owned by Pods and are
              replaced whenever the Pod is replaced.
            properties:
              networkInterfaceRef:
                minLength: 1
                type: string
              nodeName:
                minLength: 1
                type: string
              podRef:
                properties:
                  interface:
                    minLength: 1
                    type: string
                  name:
                    minLength: 1
                    type: string
                  uid:
                    minLength: 1
                    type: string
                required:
                - interface
                - name
                - uid
                type: object
            required:
            - networkInterfaceRef
            - nodeName
            - podRef
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
