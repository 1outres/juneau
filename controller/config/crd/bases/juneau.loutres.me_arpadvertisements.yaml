---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: arpadvertisements.juneau.loutres.me
spec:
  group: juneau.loutres.me
  names:
    kind: ARPAdvertisement
    listKind: ARPAdvertisementList
    plural: arpadvertisements
    singular: arpadvertisement
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.externalNetwork
      name: ExternalNetwork
      type: string
    - jsonPath: .spec.address
      name: Address
      type: string
    - jsonPath: .spec.nodeName
      name: Node
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ARPAdvertisement is the Schema for the arpadvertisements API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ARPAdvertisementSpec defines the desired state of ARPAdvertisement.
            properties:
              address:
                description: |-
                  Address is the IPv4 address answered on the external link. It must
                  fall inside one of the AddressPools behind ExternalNetwork.
                minLength: 1
                type: string
              externalNetwork:
                description: |-
                  ExternalNetwork names the ARP-mode ExternalNetwork that owns
                  Address.
                minLength: 1
                type: string
              nodeName:
                description: |-
                  NodeName is the single node that answers ARP requests for Address.
                  It is the only mutable field: a consumer rewrites it to move the
                  address to another node.
                minLength: 1
                type: string
            required:
            - address
            - externalNetwork
            - nodeName
            type: object
          status:
            description: ARPAdvertisementStatus defines the observed state of ARPAdvertisement.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
