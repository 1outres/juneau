---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: bgpnodestates.juneau.loutres.me
spec:
  group: juneau.loutres.me
  names:
    kind: BGPNodeState
    listKind: BGPNodeStateList
    plural: bgpnodestates
    singular: bgpnodestate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="BirdRunning")].status
      name: Bird
      type: string
    - jsonPath: .status.conditions[?(@.type=="BMPConnected")].status
      name: BMP
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.heartbeat
      name: Heartbeat
      priority: 1
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BGPNodeState is the Schema for the bgpnodestates API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BGPNodeStateSpec defines the desired state of BGPNodeState.
            type: object
          status:
            description: BGPNodeStateStatus defines the observed state of BGPNodeState.
            properties:
              advertisements:
                items:
                  properties:
                    addressPool:
                      type: string
                    lastSyncedAt:
                      format: date-time
                      type: string
                    prefixes:
                      description: |-
                        Prefixes is the set of CIDRs that bgp-speaker intends to advertise from
                        this AddressPool. Derived from AddressPool.spec.addresses at reconcile
                        time, not observed on the wire (BIRD BMP does not expose adj-RIB-out).
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: set
                  type: object
                type: array
              bgpSessions:
                items:
                  properties:
                    lastError:
                      type: string
                    peerAddress:
                      description: |-
                        PeerAddress is the BGP peer's IP address as observed on the wire via BMP.
                        Always set.
                      type: string
                    peerName:
                      description: |-
                        PeerName is the BGPPeer resource name that configured this session.
                        Empty when the BGPPeer resource could not be resolved (e.g. deleted
                        but session still active, or bird.conf not yet reloaded).
                      type: string
                    state:
                      type: string
                    upSince:
                      format: date-time
                      type: string
                  type: object
                type: array
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              errors:
                items:
                  properties:
                    lastSeen:
                      format: date-time
                      type: string
                    message:
                      type: string
                    resourceKind:
                      type: string
                    resourceName:
                      type: string
                  type: object
                type: array
              heartbeat:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
