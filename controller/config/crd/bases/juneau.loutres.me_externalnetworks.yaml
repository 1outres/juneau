---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: externalnetworks.juneau.loutres.me
spec:
  group: juneau.loutres.me
  names:
    kind: ExternalNetwork
    listKind: ExternalNetworkList
    plural: externalnetworks
    singular: externalnetwork
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ExternalNetwork is the Schema for the externalnetworks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExternalNetworkSpec defines the desired state of ExternalNetwork.
            properties:
              addressPools:
                items:
                  type: string
                type: array
              type:
                type: string
            type: object
          status:
            description: ExternalNetworkStatus defines the observed state of ExternalNetwork.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
